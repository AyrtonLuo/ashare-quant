venv/
.pytest_cache/
__pycache__/
*.pyc
.env

# Data Warehouse Directories — Large datasets excluded from Git
data/raw/
data/normalized/
data/validated/
data/research/
data/manifests/
*.parquet
*.db

# Streamlit secrets — API keys live in the cloud platform's own secrets store or in the local
# environment, never in the repository. Ignored so a local secrets file can never be committed.
.streamlit/secrets.toml
