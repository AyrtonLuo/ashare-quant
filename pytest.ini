[pytest]
markers =
    real_data: marks tests requiring local or real historical data pipeline
    real_provider: marks tests requiring live network provider API credentials (TuShare Pro / AkShare)
    real_llm_provider: marks tests making a real, billable LLM API call (requires OPENAI_API_KEY)
    integration: marks integration tests spanning multiple components
    unit: marks unit tests
